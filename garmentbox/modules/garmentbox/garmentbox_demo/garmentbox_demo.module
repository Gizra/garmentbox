<?php
/**
 * @file
 * Module to demonstrate the GarmentBox profile.
 */


/**
 * Group name for the migration classes.
 */
define('GARMENTBOX_DEMO_MIGRATION_GROUP', 'garmentbox:demo');


/**
 * Dummy function so we can use this in tests.
 *
 * @return string
 *   Returns hard coded string "Hello world!".
 */
function garmentbox_demo_hello_world() {
  return 'Hello world!';
}

/**
 * Implements hook_migrate_api().
 */
function garmentbox_demo_migrate_api() {
  $classes = array(
    'GarmentboxDemoCreateContent',
  );

  $migrations = array();
  foreach ($classes as $class_name) {
    $migrations[$class_name] = array(
      'class_name' => $class_name,
      'group_name' => GARMENTBOX_DEMO_MIGRATION_GROUP,
    );
  }

  $api = array(
    'api'        => 2,
    'groups'     => array(
      GARMENTBOX_DEMO_MIGRATION_GROUP => array(
        'title' => t('Demo content'),
      ),
    ),
    'migrations' => $migrations,
  );

  return $api;
}
