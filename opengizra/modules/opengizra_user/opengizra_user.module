<?php

include_once('opengizra_user.features.inc');

/**
 * Implements of hook_ctools_plugin_directory().
 */
function opengizra_user_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && !empty($plugin)) {
    return "plugins/$plugin";
  }
}