<?php
/**
 * @file
 * Code for the Opengizra Order feature.
 */

include_once 'opengizra_order.features.inc';

/**
 * Implements hook_ctools_plugin_directory().
 */
function opengizra_order_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools') {
    return 'plugins/' . $plugin;
  }
}
